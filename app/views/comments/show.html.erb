<p class="notice"><%= notice %></p>

<p>
  <b>User:</b>
  <%= @comment.user_id %>
</p>

<p>
  <b>Task:</b>
  <%= @comment.task_id %>
</p>

<p>
  <b>Body:</b>
  <%= @comment.body %>
</p>


<% if current_user and current_user.is_admin or @comment.user == current_user %>
<%= link_to 'Edit', edit_group_project_task_comment_path(@group, @project, @task, @comment) %> |
<% end %>
<%= link_to 'Back', group_project_task_path(@group, @project, @task) %>
